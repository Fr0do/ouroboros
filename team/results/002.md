# Task 002: CI — upstream sync workflow

## What was done
- Created `.github/workflows/upstream-sync.yml`
- Daily cron at 04:00 UTC + manual dispatch
- Fast-forward only merge from razzant/ouroboros:main to ouroboros-stable
- Creates branch if it doesn't exist, fails if diverged

## Files modified
- .github/workflows/upstream-sync.yml (new)

## Notes
- Requires GITHUB_TOKEN (auto-available in Actions with contents:write permission)
- No force merge — workflow fails if ouroboros-stable has diverged
