id: "004"
status: pending
title: "Add /team create command"
project: ouroboros
priority: normal
context: |
  Add a /team create subcommand to create tasks from Telegram.

  Usage: /team create <title> | <context>
  The pipe character separates title from context (context is optional).

  Example:
    /team create Fix GPU monitoring | The /gpu command times out when kurkin-4 is unreachable. Add a timeout fallback.

  Implementation:
  1. In bot/services/team.py:
     - Add next_task_id() function: reads existing task files, finds max numeric id, returns next (zero-padded 3 digits)
     - Add create_task(title, context, project="ouroboros", priority="normal") -> dict: creates task dict with all fields from LEADER.md schema, saves to file, returns dict

  2. In bot/handlers/team.py:
     - Add "create" subcommand handling in team_handler
     - Parse: args[0] == "create", rest joined as "<title> | <context>"
     - Split on " | " to get title and optional context
     - Call create_task(), reply with confirmation showing task id

  Files to modify:
  - bot/services/team.py (add next_task_id, create_task)
  - bot/handlers/team.py (add create subcommand)

  Update /start help text in bot/main.py:
  - Add: /team create <title> | <context> — create task from Telegram

  Acceptance:
  - /team create works from Telegram
  - Created YAML matches the schema in team/LEADER.md
  - Auto-increments task ID
  - Shows confirmation with task ID
depends_on: ["002"]
assigned_to: 2
created: "2026-03-09T14:00:00"
claimed_by: null
completed: null
result_file: null
